apiVersion: v1
kind: Service

metadata:
  name: k8s-cicd-service

spec:
  type: NodePort

  selector:
    app: k8s-cicd-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
